pipeline {
    agent any

    stages {
        stage("Build") {
            steps {
                echo 'We are in the build step'
            }
        }
        stage("Test") {
            steps {
                echo 'We are in the test step'
            }
        }
    }
}